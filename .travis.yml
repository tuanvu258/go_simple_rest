language: go

go:
  - "1.14"

branches:
  only:
    - master
    - staging
    - develop

jobs:
  include:
    - stage: CI    
      install:
        - go version
        - go get -u golang.org/x/lint/golint
      script:
        - echo "Hello World"
        - go build .
        - go vet .
        - golint .
        - cd test && go test -v

    - stage: Deploy
      script:
        - echo "Deploy"